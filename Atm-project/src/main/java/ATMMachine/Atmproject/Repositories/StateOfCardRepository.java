package ATMMachine.Atmproject.Repositories;

import ATMMachine.Atmproject.Models.StateOfCard;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StateOfCardRepository extends JpaRepository<StateOfCard,Long> {
}
